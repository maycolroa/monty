#include "monty.h"

/**
 * push - This function prints all the values on the stack,
 * starting from the top's stack.
 * @stack: Double pointer to the stacks's head
 * @line_number: line number.
 * Return:
 */
void push(stack_t **stack, unsigned int line_counter)